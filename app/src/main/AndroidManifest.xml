<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieTicket">

        <!-- Login Activity (Launcher) -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity android:name=".activities.RegisterActivity" android:exported="false"/>

        <!-- Main Activity - Movie List -->
        <activity android:name=".activities.MainActivity" android:exported="false"/>

        <!-- Movie Detail Activity -->
        <activity android:name=".activities.MovieDetailActivity" android:exported="false"/>

        <!-- Booking Activity -->
        <activity android:name=".activities.BookingActivity" android:exported="false"/>

        <!-- My Tickets Activity -->
        <activity android:name=".activities.MyTicketsActivity" android:exported="false"/>

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".utils.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FCM default notification channel -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="movie_ticket_channel" />

    </application>
</manifest>
